{
    IBClasses = (
        {
            ACTIONS = {chooseFile = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {statusField = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}